# Core scrapers use:
# - Go binary (./shopify-scraper) + agent-browser CLI
# - discover.py / db.py: Python 3 stdlib only (no pip deps)
#
# Install agent-browser separately:
#   npm i -g agent-browser && agent-browser install
